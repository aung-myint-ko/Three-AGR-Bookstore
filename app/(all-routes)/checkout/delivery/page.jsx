import Delivery from "@/components-checkout/Delivery";
import React from "react";

export const metadata = {
  title: "Delivery",
};

const page = () => {
  return (
    <>
      <Delivery />
    </>
  );
};

export default page;
