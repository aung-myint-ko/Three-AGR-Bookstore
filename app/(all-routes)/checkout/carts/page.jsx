import Carts from "@/components-checkout/Carts";
import React from "react";

export const metadata = {
  title: "Carts",
};

const page = () => {
  return (
    <>
      <Carts />
    </>
  );
};

export default page;
