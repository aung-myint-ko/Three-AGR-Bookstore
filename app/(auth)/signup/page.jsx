import SignUp from "@/components-auth/SignUp";
import React from "react";

export const metadata = {
  title: "Sign Up",
};

const page = () => {
  return (
    <>
      <SignUp />
    </>
  );
};

export default page;
