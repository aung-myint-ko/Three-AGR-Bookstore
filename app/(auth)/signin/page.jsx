import SignIn from "@/components-auth/SignIn";
import React from "react";

export const metadata = {
  title: "Sign In",
};

const page = () => {
  return (
    <>
      <SignIn />
    </>
  );
};

export default page;
